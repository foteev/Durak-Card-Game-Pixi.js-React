import '@testing-library/jest-dom';
import React from 'react'
// import { OurComponent } from '../OurComponent';
import { render, screen /*, waitFor, fireEvent, act*/ } from '@testing-library/react';
import { GameStage } from '../components/GameStage/GameStage'

describe('Game', () => {
  it('game stage should be visible', () => {
    // const { getByTestId } = render(<GameStage />);
    // screen
    // expect(getByTestId('start-button')).toBeInTheDocument();

  })
})